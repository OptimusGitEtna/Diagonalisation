{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Digit list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Position</th>
                <th>Valeur</th>
                <th>Polynome</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('digit_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.position }}</td>
                <td>{{ entity.value }}</td>
                <td>{{ entity.polynome }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('digit_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('digit_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('digit_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
